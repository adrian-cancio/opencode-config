# OpenCode secrets
#
# Copy this file to `.env` and fill in the real values.
# This file is loaded automatically by `plugins/dotenv.ts` (main process)
# and by the MCP launchers in `.opencode/`.

GITHUB_MCP_PAT=
CONTEXT7_API_KEY=
BRAVE_API_KEY=
OMNIROUTE_BASE_URL=
OMNIROUTE_API_KEY=
OMNIROUTE_MCP_URL=
